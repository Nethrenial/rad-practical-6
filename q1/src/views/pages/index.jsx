import DefaultLayout from "../layouts/default";

<DefaultLayout>
  <main className="home">
    <p>
      This Page can be only visited if you'are logged in <br />
      You're logged in as {email}
    </p>
  </main>
</DefaultLayout>;
